# Parameters usually enter in order "by position"
# Default parameter values allow you to omit values
# Named parameters let you skip in order
# Default and named parameters are usually options that modify function execution.

# Named and default parameters are useful for modifying how functions work
# Default values allow omission
# Named parameters allow skip


bits <- c(1, NA, 3, 4)
max(bits, na.rm = NA)
